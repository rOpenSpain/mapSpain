# esp_get_munic() supports years, filters and Canary movement

    Code
      null_res <- esp_get_munic(region = "Galicia", munic = "Melque", year = 2024,
        cache_dir = cdir)
    Condition
      Warning:
      The selected `region`, `munic` or filter combination does not return any results.
      i Returning empty <sf> object.

