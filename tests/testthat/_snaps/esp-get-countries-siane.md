# Filter countries

    Code
      db_null <- esp_get_countries_siane(country = "greenland", cache_dir = cdir)
    Message
      ! The values in `country` do not return any results.
      i Returning empty <sf> object.

