# esp_get_attributions() handles custom and built-in providers

    Code
      nn <- esp_get_attributions(custom_wmts)
    Condition
      Warning:
      No attribution found for provider "MadridMapBox".

